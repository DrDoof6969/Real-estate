{
  "name": "deal-qualifier",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted rental deal qualifier with a RentCast address lookup, shared caching, spend budgets and bot protection.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node test/smoke.test.js"
  }
}
